import { Style } from 'geostyler-style';

const polygonSimpleNew: Style = {
  "name": "QGIS Style",
  "rules": [{
    "name": "QGIS Simple Symbol",
    "symbolizers": [{ "kind": "Fill" }]
  }]
} as Style;

export default polygonSimpleNew;
